---
// Section H2 (Variant B). Content provided via slot.
---

<h2 class="heading"><slot /></h2>

<style>
  .heading {
    font-family: var(--font-display);
    font-size: clamp(32px, 4vw, 60px);
    font-weight: 600;
    letter-spacing: -0.032em;
    color: var(--t1);
    margin-bottom: 44px;
  }

  @media (max-width: 768px) {
    .heading {
      margin-bottom: 28px;
    }
  }
</style>
